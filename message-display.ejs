<!DOCTYPE html>
<html lang="en">

      <ul class='posts'>
        <% for(var i= 0 ; i<posts.length; i++) {%>
          <li class='posts'>

            <span><%= posts[i].title %></span><br>
            <span><%= 'Subject: ' + posts[i].subject %></span><br>
            <span><p><%=  posts[i].blog  %></p></span>
          </li>
          <% } %>
      </ul>
      </html>   